// important to note that when using ng build angular will replace environment import the correct environment file based on the build configuration
// of --configuration = production

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
}
